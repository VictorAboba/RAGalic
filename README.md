# DRAG with KNEE

Система Retrieval-Augmented Generation (RAG) для работы с PDF-документами с иерархической структурой и гибридным поиском.

## Возможности

- **Извлечение текста из PDF** — использует Docling с OCR (EasyOCR) для распознавания текста с отсканированных документов
- **Иерархическое индексирование** — автоматическое построение дерева документа с агрегацией метаданных на разных уровнях
- **Гибридный поиск** — комбинирует dense (семантический) и sparse (BM25) векторный поиск с использованием RRF-фьюжина
- **Beam Search** и **Branch Search** — алгоритмы поиска для навигации по иерархической структуре документа
- **Knee-методы** — автоматическое определение оптимального количества результатов через анализ точки максимального изгиба графика релевантности
- **LLM-метаданные** — генерация описаний и ключевых слов с помощью больших языковых моделей

## Установка

1. Клонируйте репозиторий и перейдите в директорию проекта:
```bash
cd DRAG-with-KNEE
```

2. Установите зависимости через uv:
```bash
uv sync
```

3. Скопируйте файл конфигурации:
```bash
cp .env.example .env
```

4. Настройте переменные окружения в `.env`:
```
API_KEY="ваш_api_ключ"
URL_BASE="https://api.proxyapi.ru/openrouter/v1"
MODEL="z-ai/glm-4.7-flash"
FASTEMBED_CACHE_PATH="./fastembed_cache"
```

## Использование

### Индексация документа

Для индексирования PDF-документа используйте функцию `build_tree`:

```python
from pathlib import Path
from rag_lib.build_tree import build_tree

path = Path("path/to/your/document.pdf")
build_tree(path, width=3, batch_size=5)
```

Параметры:
- `width` — количество дочерних узлов, объединяемых в родительский (по умолчанию 3)
- `batch_size` — размер батча для записи в БД (по умолчанию 5)

### Поиск по документам

#### Branch Search

Иерархический поиск, начинающий с корневых узлов:

```python
from rag_lib.search import branch_search

results = branch_search(query="ваш_запрос", num_roots=3)
for chunk in results:
    print(f"{chunk.file_name} (страницы {chunk.page_start}-{chunk.page_end}):\n{chunk.text[:200]}...")
```

#### Beam Search

Расширенный поиск с итеративным сужением:

```python
from rag_lib.search import beam_search

# Фиксированная ширина луча
results = beam_search(query="ваш_запрос", beam_width=3, search_method="fixed")

# Адаптивная ширина с автоматическим определением через knee-метод
results = beam_search(
    query="ваш_запрос",
    search_method="adaptive_with_knee",
    max_num_roots=20  # максимальное количество корневых узлов для анализа
)

# Адаптивная ширина с настраиваемой чувствительностью knee-метода
results = beam_search(
    query="ваш_запрос",
    search_method="adaptive_with_sensitive_knee",
    sensitivity=0.85,  # чувствительность knee-отсечения (0.0-1.0)
    max_num_roots=20
)
```

Параметры:
- `beam_width` — фиксированная ширина луча (для метода `"fixed"`)
- `search_method` — режим работы:
  - `"fixed"` — фиксированная ширина луча
  - `"adaptive_with_knee"` — автоматическое определение через knee-точку
  - `"adaptive_with_sensitive_knee"` — адаптивный метод с настраиваемой чувствительностью
- `max_num_roots` — максимальное количество корневых узлов для анализа при адаптивном методе (по умолчанию 20)
- `sensitivity` — чувствительность для метода `"adaptive_with_sensitive_knee"` (по умолчанию 0.85). Чем выше значение, тем агрессивнее отсекается "хвост" результатов

## Архитектура алгоритмов

### 1. Извлечение текста (Chunking)

Проект использует [Docling](https://www.docling.ai/) — высокоточный конвертер документов с поддержкой:
- Извлечения текста из PDF
- OCR через EasyOCR для отсканированных документов
- Распознавания таблиц

Текст обрабатывается от переносов строк (`fix_hyphenation`), после чего разбивается постранично.

### 2. Построение дерева документа

Алгоритм `build_tree` создаёт иерархическую структуру:

1. **Листья (страницы)** — каждая страница документа становится листом, для неё LLM генерирует:
   - `description` — краткое техническое описание (3-5 предложений)
   - `keywords` — 5-10 ключевых слов для BM25-поиска

2. **Агрегация в родительские узлы** — каждые `width` узлов объединяются в родительский, LLM генерирует обобщающее описание на основе метаданных дочерних узлов

3. **Корневой узел** — вершина дерева с `parent_id = -1`, представляющая весь документ

### 3. Векторное индексирование

Используется [Qdrant](https://qdrant.tech/) с гибридным поиском:

- **Dense embeddings** — `jinaai/jina-embeddings-v3` для семантического поиска
- **Sparse embeddings** — BM25 для точного ключевого поиска

Векторы хранятся в отдельных пространствах (`dense` и `sparse`) и объединяются через **Reciprocal Rank Fusion (RRF)**:

```
RRF_score(doc) = Σ 1 / (k + rank(doc))
```

где `k = 60` (константа сглаживания).

### 4. Поисковые алгоритмы

#### Branch Search

```
1. Найти корневые узлы (top-k) гибридным поиском
2. Для каждого корня:
   a. Сравнить родителя с его детьми
   b. Если дети релевантнее — добавить их в следующую итерацию
   c. Если родитель лучше детей — это финальный результат
3. Повторять до тех пор, пока есть более релевантные дети или достигли листа.
```

#### Beam Search

Beam Search поддерживает три режима работы:

**Режим `"fixed"` (фиксированная ширина):**
```
1. Найти корневые узлы (beam_width)
2. Итеративно:
    a. Сравнить всех родителей и их детей
    b. Применить сужение (suppression)
    c. Ограничить результаты до beam_width
3. Вернуть финальные узлы
```

**Режим `"adaptive_with_knee"` (адаптивная ширина):**
```
1. Найти все корневые узлы (до max_num_roots)
2. Определить оптимальное количество через knee-точку (cut_knee)
3. Итеративно:
    a. Сравнить всех родителей и их детей
    b. Применить knee-отсечение для автоматического выбора количества
    c. Применить сужение (suppression)
4. Вернуть финальные узлы
(В данном случае порядок `b` и `c` оказывается очень важен, так как помогает стабилизироваться алгоритму не на 2 двух точках)
```

**Режим `"adaptive_with_sensitive_knee"` (адаптивная ширина с настраиваемой чувствительностью):**
```
1. Найти все корневые узлы (до max_num_roots)
2. Определить оптимальное количество через гибкий knee-метод (cut_knee_flexible)
3. Итеративно:
    a. Сравнить всех родителей и их детей
    b. Применить гибкое knee-отсечение для автоматического выбора количества
    c. Применить сужение (suppression)
4. Вернуть финальные узлы
```

**Логика сужения (Suppression):**
- Если ребёнок лучше родителя → исключаем родителя
- Если родитель лучше детей → исключаем всех детей

**Knee-методы:**

1. **`cut_knee`** — базовый алгоритм автоматического определения оптимального количества результатов на основе анализа графика RRF-скоров. Находит точку максимального изгиба (колено) — место, где происходит резкое снижение релевантности.

2. **`cut_knee_flexible`** — расширенная версия с настраиваемой чувствительностью. Параметр `sensitivity` (0.0-1.0) контролирует, насколько агрессивно отсекается "хвост" результатов. Чем выше значение, тем агрессивнее обрезается хвост (при значении 1 идентично стандартному `cut_knee`)

Оба метода используют нормированное расстояние от точки до прямой, соединяющей первый и последний элементы списка результатов.

## Структура проекта

```
DRAG-with-KNEE/
├── rag_lib/
│   ├── build_tree.py    # Построение дерева документа
│   ├── chunking.py      # Извлечение текста из PDF
│   ├── clients.py       # Подключение к Qdrant и LLM
│   ├── config.py        # Конфигурация
│   ├── dataschemes.py   # Pydantic-схемы
│   ├── search.py        # Поисковые алгоритмы
│   ├── utils.py         # Утилиты для LLM-вызовов
│   ├── database/
│   │   └── parsed_files/  # JSON-дампы обработанных документов
│   └── qdrant_db/       # Векторная база данных
├── .env                 # Конфигурация (не в репозитории)
├── pyproject.toml       # Зависимости проекта
└── README.md
```

## Зависимости

- **docling** — извлечение текста из PDF
- **easyocr** — OCR для отсканированных документов
- **qdrant-client[fastembed]** — векторная БД с гибридным поиском
- **openai** — API для LLM
- **rich** — красивый CLI-вывод
